## タスク
- ID: impl-reverse
- 内容: Milestone 2 ステップ G - reverse の実装とテスト

## 作成ファイル
- `src/toy/reverse.py` - reverse_string, reverse_words の実装
- `tests/toy/test_reverse.py` - 正常系・境界値テスト 9 件

## 設計判断
- 設計ドキュメント通り、reverse_string は `s[::-1]`、reverse_words は `split()` + `[::-1]` + `join` で実装
- `split()` 引数なしで連続空白・先頭末尾空白を自動処理

## 次のタスクへの申し送り
- 特になし。シンプルな実装で問題なし
