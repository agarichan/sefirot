# Milestone 1: toy パッケージのディレクトリ構造セットアップ

## 概要
`src/toy/` と `tests/toy/` のディレクトリおよび `__init__.py` を作成し、`pytest tests/toy/` が 0 テストで正常終了する状態にする。

## タスク一覧

| ステップ | タスクID | Wave | 概要 |
|---------|---------|------|------|
| A | setup-toy-directories | 1 | src/toy/ と tests/toy/ のディレクトリ・__init__.py を作成 |

## タスク詳細

### ステップ A: setup-toy-directories（Wave 1）

#### 作成するファイル
- `src/toy/__init__.py`
- `tests/toy/__init__.py`

#### 型定義・インターフェース
なし。両ファイルとも空ファイル（0 バイト）で作成する。

#### import 先（既存コード）
なし。

#### 実装パターン
- `src/sefirot/__init__.py` と同様に空の `__init__.py` を配置する
- ディレクトリが存在しない場合は作成する

#### 注意事項
- `pytest tests/toy/` が正常終了すること（exit code 5 = "no tests collected" は正常終了扱い）
- `pyproject.toml` の `testpaths = ["tests"]` に `tests/toy/` は含まれるため、設定変更は不要
