# toy プログラムへの新機能追加

## 目的

sefirot の質問キュー機能を検証するため、Builder が仕様不明点についてユーザーに質問を投げるケースを意図的に発生させる。

## 概要

既存の toy プログラム（`src/toy/` 配下）のいずれかに新機能を追加する。

追加する機能の詳細は未定。Builder は設計者の意図を確認してから実装に着手すること。

## タスク

### Step A: `add-toy-feature` (Wave 1)

#### 対象ファイル

- `src/toy/` 配下のいずれかのモジュール
- `tests/toy/` 配下の対応するテストファイル

#### 要件

- 既存の toy プログラムに有用な機能を追加する
- テストを書く

#### 未決事項

- どのプログラムに機能を追加するか
- どんな機能を追加するか
